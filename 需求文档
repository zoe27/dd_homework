钉钉作业 Bot 系统需求文档

一、项目目标

在个人钉钉群中部署一个 Bot，用户将家校本作业卡片转发到该群后，
通过 @bot 指令触发作业汇总与打印。

二、核心流程

1. 用户在家校本查看各科老师发布的作业
2. 将作业卡片逐一转发到个人建的小群（群里只有自己和 Bot）
3. 发送 "@bot 总结打印"
4. Bot 读取群里最近的转发消息，提取所有作业内容（文字 + 图片）
5. 生成 Word 文档（./output/今日作业_YYYYMMDD.docx）
6. 自动发送打印任务到系统默认打印机
7. 群里回复打印确认

三、输入格式（家校本卡片）

家校本卡片格式固定，标题为"日期+科目"，内容为编号列表，可附图片：

  4月10日数学
  1订正知能P17,P18
  2完成知能P19,P20
  [图片附件]

  4月10日语文
  1背诵第三课课文
  2完成练习册第5页

特点：
  - 标题格式：M月D日+科目名（无空格）
  - 作业内容：数字编号开头，紧跟内容，无分隔符
  - 可能附带课本照片

四、输出

1. Word 文档（保留在 ./output/ 目录）
   - 标题：今日作业 / 日期
   - 按科目分块：科目标题 + 作业列表 + 图片
   - 文件名：今日作业_20260410.docx

2. 打印输出
   - 调用系统默认打印机
   - macOS：lp 命令
   - Windows：win32print

3. 群回复
   - 打印成功：✓ 已打印，共 N 科 M 条作业
   - 打印失败：提示错误，文档已保存到 ./output/

五、功能模块

1. Bot 模块（bot/）
   - 通过 DingTalk Stream SDK 接入
   - 监听群内所有消息，缓存最近 N 条
   - 识别 @bot 总结打印 指令，触发处理流程

2. 解析模块（parser/）
   - 解析家校本卡片文本，提取日期、科目、作业条目
   - 收集关联图片的下载地址

3. 文档生成模块（generator/）
   - 使用 python-docx 生成 Word 文档
   - 支持文字排版 + 图片嵌入

4. 打印模块（printer/）
   - 跨平台打印（macOS / Windows）
   - 打印失败不崩溃，保留文档

六、系统约束

- Bot 运行在本机（无需公网服务器，Stream SDK 主动连接钉钉）
- 图片通过钉钉 API 下载到本地临时目录后嵌入文档
- 仅处理文字和图片，忽略其他附件类型

七、扩展方向（后续迭代）

- 打印后自动删除 Word 文件
- 支持指定日期回溯（@bot 总结打印 4月10日）
- 支持预览模式（@bot 预览），在群里直接回复作业摘要
